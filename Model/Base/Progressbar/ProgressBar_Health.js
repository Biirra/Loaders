class ProgressBar_Health extends Base_ProgressBar{
    constructor(name){
        super(name);
        this.foreGroundColor = new Color(255, 0, 0);			// color foreground.
		this.backGroundColor = new Color(0, 0, 0);				// color background.
    }
    update(){
        
    }
    getSpeed(){
        // return a value created from 
    }
    addOverTime(value){

    }
    subOverTime(value){

    }
}