class ProgressBar_Mana extends Base_ProgressBar{
    constructor(name){
        super(name);
        this.foreGroundColor = new Color(0, 0, 230, 1);			// color foreground.
		this.backGroundColor = new Color(0, 0, 0);				// color background.
    }
    update(){
        
    }
    getSpeed(){
        // return a value created from 
    }
    addOverTime(value){

    }
    subOverTime(value){

    }
}