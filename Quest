Quest:
    contains:
        Encounter:
            encounters. these are all the encounters the player must complete to complete the quest.
            
        
        start encounter: 
            user needs to either accept or decline this encounter.
            accept(), the quest starts as intenden.
            decline(), the user declines the quest.

        reward encounter: 
            the encounter that contains the the loot for the quest.
            takeItems(), The user takes the reward from the quest.
            skip(), user does not take items and resumes instead.
            
